#include <stdio.h>
#include <math.h>
sy(int n) {
	int a=0,sum=0,i;
	while(n) {
		a=n%2;
		a=n/2;
		sum+=a*pow(10,i);
		i++;
		return (sum);
	}
}

int main() {
	int i = 1,l,r;
	scanf("%d",&l);
	scanf("%d",&r);
	int	xy(int l);
	int	xy(int n);
	while (1) {
		i = i << 1;
		i = i + 1;
		if(l<i&&i<r) {
			printf("%d",i);
			break;
		}
	}
}
